import 'package:flutter/material.dart';

// Define constants that are used in multiple places in the app

const Color primaryColor = Color.fromARGB(255, 16, 125, 215);
const Color secondaryColor = Color.fromARGB(255, 34, 227, 211);
const Color accentColor = Colors.lightBlueAccent;
